name: .NET

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Install NBGV
      run: dotnet tool install -g nbgv
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore -c Release
    - name: Set Nuget package version
      run: |
        npkgVersion=$(nbgv get-version -v NugetPackageVersion) \
        npkgFilePath=$(find ${GITHUB_WORKSPACE}/osocli/bin/Release -type f -name "*.nupkg") \
        dotnet nuget push $npkgFilePath --api-key ${{ secrets.NUGET_KEY }} --source https://api.nuget.org/v3/index.json
      
